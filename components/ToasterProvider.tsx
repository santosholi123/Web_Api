"use client";

import React from "react";

/**
 * ToasterProvider - Placeholder for toast notification system
 * In production, you would integrate a toast library like react-hot-toast or sonner
 */

export function ToasterProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
